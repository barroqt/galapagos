[package]
name = "sim-core"
version = "0.1.0"
edition = "2021"
description = "Evolutionary game theory simulation core (compiled to WASM)"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
rand = { version = "0.8", default-features = false, features = ["small_rng"] }
